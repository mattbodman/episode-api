{
  "name": "episode-api",
  "version": "1.0.1",
  "description": "A NodeJS wrapper for the Telestream Episode Engine JSON API",
  "main": "episodeInstance.class.js",
  "scripts": {
    "test": "mocha *test* --recursive"
  },
  "author": "Matt Bodman",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0",
    "request-promise-native": "^1.0.5"
  },
  "devDependencies": {
    "mocha": "^4.0.1",
    "pretty-data": "^0.40.0",
    "supertest": "^3.0.0"
  }
}
